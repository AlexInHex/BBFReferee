@model IEnumerable<UserViewModel>

@{
    ViewData["Title"] = "User";
}

<div class="list-group">
    @foreach (var user in Model)
    {
        <a asp-action="List" asp-route-id="@user.Id" class="list-group-item list-group-item-action">@user.Name</a>
    }
</div>

